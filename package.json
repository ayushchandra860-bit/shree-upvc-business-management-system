{
  "name": "shree-upvc-business-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready UPVC windows and doors business management system for SHREE UPVC WINDOWS & DOORS.",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server",
    "seed:admin": "node server/scripts/create-admin.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
